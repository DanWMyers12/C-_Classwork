#include <iostream>
#include <string>

using namespace std;

const int maxArray = 20;

int base2Dec(int base, const char num[maxArray]) // takes origin base and number as a char[] and converts it to an int base 10

char[maxArray] dec2Base(int base, int num) // takes target base and base 10 number and reads it into an aray as digits;

int charCoder(char a) // takes character of string and returns a base ten value
char charCoder(int a) // takes int < 16 and returns a character for base 2 -16 as a digit value



int main()
{
	string input;
	char num[maxArray];
	int originBase;
	int targetBase;
	
	cin >> input;
	for(int i =0;i<input.size())

	return 0;
}